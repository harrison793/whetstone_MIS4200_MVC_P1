@model IEnumerable<whetstone_MIS4200_MVC_P1.Models.appointmentDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.unitCost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.procedureID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.productID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.unitCost)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.procedureID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.productID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.appointmentDetailID }) |
            @Html.ActionLink("Details", "Details", new { id=item.appointmentDetailID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.appointmentDetailID })
        </td>
    </tr>
}

</table>
